{"category": "Fixed", "changelog_entry": "Resolved the Tekton operator installation issue by updating its link in the install script to point to `infra.tekton.dev` instead of `storage.googleapis.com`, requiring users to verify their setup after updating if they had previously installed it.", "summary": "The Tekton operator link was updated in the install script to point to a new location, now referencing `infra.tekton.dev` instead of `storage.googleapis.com`. This change affects how the Tekton operator is installed and may require manual intervention for existing installations. The installation process remains largely unchanged, but users should verify their setup after updating.", "version": 1}
