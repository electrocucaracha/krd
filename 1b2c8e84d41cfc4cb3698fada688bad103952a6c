{"category": "Added", "changelog_entry": "Enabled support for Ubuntu Noble runners by updating pipeline runs and RBAC configuration to streamline deployment and enhance security and flexibility for GitHub Actions integration with KubeVirt.", "summary": "Ubuntu Noble runners are now supported, requiring updates to pipeline runs, VM templates, and RBAC configuration. Existing deployments must update RBAC and VM resource names to match the new structure due to breaking changes. The RBAC setup has been refactored to use ClusterRole for kubevirt-actions-runner, granting necessary permissions across namespaces. A dedicated values file is used for VM-based self-hosted runners, improving maintainability and clarity.", "version": 1}
