{"category": "Changed", "changelog_entry": "Enabling namespace deployments for arc allows users to deploy runners in their own custom namespaces instead of the default one, affecting API behavior by allowing helm installation and deployment in these custom namespaces.", "summary": "Namespace deployments for arc are now allowed, enabling users to deploy runners in their own namespace instead of the default one. This change affects API behavior by allowing helm installation and deployment in custom namespaces. Additionally, it introduces config schema changes to accommodate the new deployment model. Users will need to migrate their existing setups to utilize this feature.", "version": 1}
