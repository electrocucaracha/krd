{"category": "Changed", "changelog_entry": "Enabled debugging for workflows by defaulting KRD_DEBUG to ACTIONS_RUNNER_DEBUG, which in turn defaults to ACTIONS_STEP_DEBUG if set, otherwise falling back to false.", "summary": "The debug environment variable KRD_DEBUG now defaults to the value of ACTIONS_RUNNER_DEBUG, which in turn defaults to ACTIONS_STEP_DEBUG if set, otherwise defaulting to false. This change affects how users can enable debugging for their workflows. The config schema has been updated accordingly.", "version": 1}
