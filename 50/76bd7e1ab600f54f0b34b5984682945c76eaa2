{"category": "Added", "changelog_entry": "Enabled increased inotify resources to prevent failures on fsnotify watcher when running `kubectl logs`, particularly benefiting users relying on Rook Ceph functionality.", "summary": "Increased inotify resources to prevent failures on fsnotify watcher when running `kubectl logs`. This change affects users who rely on `kubectl logs` functionality, particularly those using Rook Ceph. The setup_sysctl function now sets three kernel values: max_user_watches, max_user_instances, and max_queued_events, all with a value of 2099999999. These changes are intended to prevent inotify resource exhaustion issues.", "version": 1}
