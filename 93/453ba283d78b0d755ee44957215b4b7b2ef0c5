{"category": "Added", "changelog_entry": "Enabled specification of the default provider for Vagrant through the introduction of the VAGRANT_DEFAULT_PROVIDER environment variable, which now defaults to \"virtualbox\".", "summary": "The VAGRANT_DEFAULT_PROVIDER environment variable was defined to specify the default provider for Vagrant, which affects how virtual machines are provisioned. This change impacts users who rely on Vagrant and have not previously set this variable. The new default provider is set to \"virtualbox\" through a write_files directive in the ubuntu-runner-pipeline.yml file.", "version": 1}
