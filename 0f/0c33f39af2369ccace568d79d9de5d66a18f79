{"category": "Changed", "changelog_entry": "Increased the timeout for Longhorn tests to 5 minutes from the default of 1 minute, allowing persistent volume claim binding and potentially requiring adjustments to test scripts that relied on the previous duration.", "summary": "Longhorn tests now have an increased timeout to wait for persistent volume claim binding, changing the `kubectl wait` command's default timeout from 1 minute to 5 minutes. This change affects test completion times and may require adjustments to test scripts that rely on the previous timeout duration. The overall behavior of Longhorn remains unchanged with this update.", "version": 1}
