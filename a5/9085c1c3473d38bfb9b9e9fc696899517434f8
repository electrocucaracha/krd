{"category": "Removed", "changelog_entry": "Simplified the VBox logging during Vagrant up actions to reduce unnecessary log retrieval from VirtualBox VMs without introducing any breaking behavior and maintaining compatibility with existing workflows.", "summary": "VBox logging has been reduced in the Vagrant up action, removing unnecessary log retrieval from VirtualBox VMs. This change does not introduce any breaking behavior and maintains compatibility with existing workflows. The updated action now only lists running VMs as before, streamlining the process without compromising functionality or security.", "version": 1}
