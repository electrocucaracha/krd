{"category": "Added", "changelog_entry": "Introduced support for Python-based workflows by installing essential packages in the Ubuntu runner pipeline, requiring no migration steps from existing pipelines due to a non-breaking change.", "summary": "The Ubuntu runner pipeline now installs Python packages, including python3, python3-dev, python3-pip, and python3-venv, to support Python-based workflows. This change introduces breaking behavior for users relying on the previous default Python version. The pipeline's config schema has been updated to accommodate these new dependencies, requiring no migration steps from existing pipelines.", "version": 1}
