{"category": "Added", "changelog_entry": "Enabled secure execution of GitHub Actions workflows by authenticating with a repository-specific secret token instead of personal access tokens.", "summary": "The GitHub Actions workflow now uses a secret token to authenticate with the repository, replacing a personal access token. This change ensures secure execution of commands in workflows without exposing sensitive credentials. The WORKFLOW_TOKEN secret must be set up and stored securely for this update to function correctly.", "version": 1}
