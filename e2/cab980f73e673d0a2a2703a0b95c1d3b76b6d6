{"category": "Changed", "changelog_entry": "The arc garbage collector now periodically deletes succeeded virtual machine instances in addition to ephemeral runners.", "summary": "The arc garbage collector now includes removal of virtual machine instances in its cleanup process, which previously only removed ephemeral runners. This change adds a new CronJob to periodically delete succeeded virtual machine instances and updates related ClusterRoles and ClusterRoleBindings for the cleaner ServiceAccount. Migration is not required as this is an enhancement to existing functionality.", "version": 1}
