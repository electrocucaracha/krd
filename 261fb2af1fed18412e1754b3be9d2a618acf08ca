{"category": "Added", "changelog_entry": "Enabled trivy severity filtering allowing users to specify which vulnerability severities are checked during scans impacting the trivy.yaml configuration file where users can now define which severities to include CRITICAL and HIGH by default.", "summary": "Trivy severity filtering was added, allowing users to specify which vulnerability severities are checked during scans. This change impacts the trivy.yaml configuration file, where users can now define which severities to include (CRITICAL and HIGH by default). The super-linter Makefile has been updated to validate this new configuration.", "version": 1}
