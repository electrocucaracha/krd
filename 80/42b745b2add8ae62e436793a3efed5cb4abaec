{"category": "Added", "changelog_entry": "Disabled gvisor by default in CRI-O runtime manager for users relying on this feature, breaking compatibility with gvisor for CRI-O deployments in Kubernetes 1.24 and later versions.", "summary": "CRI-O runtime manager now disables gvisor by default when the container runtime is set to crio, affecting users who rely on this feature. This change breaks compatibility with gvisor for CRI-O deployments in Kubernetes 1.24 and later versions. The config schema remains unchanged; no migration steps are required.", "version": 1}
