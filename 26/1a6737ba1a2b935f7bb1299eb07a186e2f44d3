{"category": "Fixed", "changelog_entry": "Resolved compatibility issues by downgrading default containerd version to 2.2.3 which restores stability for users relying on the default environment configuration without introducing any breaking behavior or API changes.", "summary": "The default containerd version was downgraded from 2.3.2 to 2.2.3 due to compatibility issues with dependent components that have not been updated for this release, restoring stability for users relying on the default environment configuration. This change does not introduce any breaking behavior or API changes but requires migration of existing configurations to use the new version. The security impact is minimal as it only affects the default containerd version and does not involve any security patches.", "version": 1}
