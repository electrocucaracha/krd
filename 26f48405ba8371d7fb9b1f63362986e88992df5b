{"category": "Fixed", "changelog_entry": "Stabilized the Vagrantfile's free memory output to standard error, preventing diagnostic information from interfering with scripts that expect clean stdout and ensuring compatibility with automation tools.", "summary": "The Vagrantfile now prints free memory information to standard error instead of standard output, ensuring compatibility with automation tools that expect clean stdout. This change prevents diagnostic information from polluting stdout and interfering with scripts. The underlying memory calculation logic remains unchanged.", "version": 1}
