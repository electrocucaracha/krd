{"category": "Changed", "changelog_entry": "The update version script now automatically re-formats code on exit to maintain consistency and cleanliness.", "summary": "The update version script has been improved by adding a trap to run the make fmt command on exit, ensuring that the code remains formatted correctly after running the script. This change does not introduce any breaking behavior and maintains compatibility with existing workflows. The primary user outcome is a more robust and maintainable codebase.", "version": 1}
