{"category": "Changed", "changelog_entry": "CI workflows now support testing Youki-enabled environments alongside other runtimes like crio and containerd without requiring any migration steps for existing workflows.", "summary": "The youki runtime has been enabled in the CI workflow, allowing for testing of Youki-enabled environments alongside other runtimes like crio and containerd. This change adds a new input parameter \"youki-enabled\" to the Vagrant-up action and updates the on-demand CI workflow matrix to include Youki as an option. The primary user outcome is that tests can now be run with Youki enabled, but no migration steps are required for existing workflows.", "version": 1}
