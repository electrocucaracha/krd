{"category": "Changed", "changelog_entry": "Hardened KubeVirt Runner to allow for increased virtual machine density by enabling memory overcommit and reducing the default memory request from 16G to 8G, with guest memory allocation set to 16G.", "summary": "Memory overcommit was enabled on KubeVirt Runner, allowing for increased virtual machine density by allocating less memory upfront but risking performance degradation if actual usage exceeds allocated amounts. The default memory request was reduced from 16G to 8G and the guest memory allocation was set to 16G. This change may require users to monitor their VMs' resource utilization more closely.", "version": 1}
