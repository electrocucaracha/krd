{"category": "Changed", "changelog_entry": "The Kubernetes version is now correctly retrieved and stored in cluster configuration files regardless of the presence of the KRD_KUBE_VERSION variable or a cluster configuration file.", "summary": "The code that retrieves the Kubernetes version has been updated to handle different scenarios correctly, including when the `KRD_KUBE_VERSION` variable is set or when the cluster's configuration file exists. This change ensures that the correct version is used in various situations, and it also fixes a bug where an incorrect version was being returned in certain cases. The update affects how Kubernetes versions are retrieved and stored in the cluster's configuration files.", "version": 1}
