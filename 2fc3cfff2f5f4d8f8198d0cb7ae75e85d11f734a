{"category": "Added", "changelog_entry": "Enabled pinned actions in the version update script to allow certain GitHub Actions to remain at specific versions while others are updated automatically, preventing breaking changes from automatic updates unless explicitly removed from a new `pinned_actions` array.", "summary": "The version update script for GitHub Actions now supports pinned actions, allowing certain actions to remain at specific versions while others are updated automatically. This introduces a new `pinned_actions` array and logic to skip auto-updating listed actions. Breaking changes from automatic updates are prevented, but other actions can still be regularly updated. The change also adds documentation on when and why actions are pinned.", "version": 1}
