{"category": "Changed", "changelog_entry": "Updated the Ubuntu runner pipeline to utilize VirtualBox 7.2 instead of version 7.1, requiring users to ensure their existing environments and VirtualBox installations are updated accordingly.", "summary": "The Ubuntu runner pipeline has been updated to use VirtualBox 7.2 instead of version 7.1, which may require users to update their existing environments if they were relying on the previous version. This change does not affect the API or CLI contract and there are no security implications. Users will need to ensure that their VirtualBox installation is up-to-date with the new version.", "version": 1}
