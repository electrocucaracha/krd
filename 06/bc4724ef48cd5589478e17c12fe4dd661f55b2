{"category": "Added", "changelog_entry": "The Ubuntu runner pipeline now uses a passwordless sudo account, allowing for automated tasks without user intervention.", "summary": "The Ubuntu runner pipeline now uses a passwordless sudo account, allowing for automated tasks without user intervention. This change affects the security of the pipeline by reducing the need for explicit passwords in scripts. The config schema has been updated to include the new sudo settings, and users should review their pipeline configurations for any potential issues.", "version": 1}
