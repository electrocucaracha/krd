{"category": "Added", "changelog_entry": "Enabled local network access for LiteLLM by default, allowing Firefox and Chrome to connect without requiring system settings changes, improving usability for users accessing LiteLLM from these browsers.", "summary": "Local network access for LiteLLM has been enabled by default, allowing Firefox and Chrome to connect without requiring system settings changes. This change affects the ingress configuration in the litellm.yml file. No breaking behavior or migration requirements are introduced, and no API or CLI contract changes have occurred. The primary outcome is improved usability for users accessing LiteLLM from local networks with these browsers enabled.", "version": 1}
