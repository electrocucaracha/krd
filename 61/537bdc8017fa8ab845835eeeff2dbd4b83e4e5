{"category": "Changed", "changelog_entry": "Enabled textlint with the terminology rule, enforcing consistent formatting and spelling across the project.", "summary": "This commit introduces textlint configuration and improves automation. It adds .textlintrc.json and .textlintignore files to support textlint with the terminology rule, updates the Makefile to run textlint during formatting, and ensures necessary npm packages are installed only when missing. Additionally, it standardizes spellings in the CHANGELOG.md and updates the bindep.txt to use consistent package casing.", "version": 1}
