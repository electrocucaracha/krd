{"category": "Changed", "changelog_entry": "Credentials are now persisted for subsequent jobs in the update CI workflow.", "summary": "PR creation in the update CI workflow has been fixed by changing persist-credentials to true, which will allow credentials to be persisted for subsequent jobs. This change affects how credentials are handled and stored during the workflow execution. The update does not introduce any breaking behavior or migration requirements.", "version": 1}
