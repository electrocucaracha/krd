{"category": "Added", "changelog_entry": "Enabled the Ubuntu runner pipeline to install and utilize the git package on each run without requiring any migration steps from users who rely on it for tasks involving Git repositories.", "summary": "The Ubuntu runner pipeline now includes the git package, which will be installed on each run. This change affects users who rely on the runner for tasks involving Git repositories, requiring no migration steps but potentially impacting build scripts that assume certain packages are not present. The addition of git to the list of installed packages is a minor update with no security implications or changes to the config schema.", "version": 1}
