{"category": "Removed", "changelog_entry": "Eliminated reliance on cache CI task for Vagrant boxes builds which may now take longer to complete without the ability to reuse cached box files.", "summary": "The cache CI task for Vagrant boxes was removed from the workflow, which may break builds that relied on this task to speed up testing by reusing cached box files. This change does not affect the API or CLI contract and there are no security implications or config schema changes. Users will need to ensure their workflows can handle slower build times without caching.", "version": 1}
