{"category": "Removed", "changelog_entry": "Simplified runner provisioning by eliminating redundant configuration elements and clarifying the setup flow.", "summary": "Runner setup has been streamlined by consolidating initialization steps under runcmd, removing redundant Vagrant box additions, and adding explicit sync, sleep, and shutdown commands for clean shutdown. Power state configuration now uses a delayed shutdown with an updated message and increased timeout for reliability. Unused Service and EndpointSlice definitions have been removed to reduce clutter and potential confusion in the runner provisioning flow.", "version": 1}
